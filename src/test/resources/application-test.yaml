kafka:
  bootstrap-servers: ${spring.embedded.kafka.brokers}

spring:
  kafka:
    bootstrap-servers: ${spring.embedded.kafka.brokers}
#    streams:
#      bootstrap-servers: ${spring.embedded.kafka.brokers}

bootstrap:
  servers: ${spring.embedded.kafka.brokers}